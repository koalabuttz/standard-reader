/target
**/*.rs.bk
Cargo.lock.orig

# Local runtime state (cache DB, cached blobs, OAuth session). Never commit.
/data
*.redb

# Local working notes / design scratch — not part of the published repo.
/scratch

# Web (trunk/wasm) build output + cache.
dist/
.trunk/

# Stray screenshots dropped in the repo (e.g. shared during dev).
/Screenshot*.png
