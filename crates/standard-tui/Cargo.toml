[package]
name = "standard-reader"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "A lean, polished TUI reader for standard.site (long-form on the AT Protocol)."

[[bin]]
name = "sr"
path = "src/main.rs"

[dependencies]
standard-core = { path = "../standard-core" }
# Frontend deps (ratatui, crossterm, ratatui-image, reqwest, tokio, redb,
# atrium-oauth, pulldown-cmark) land as the TUI is built up.
